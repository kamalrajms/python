29/10/2025

    python ?

    high level language
    interperted -- line by  line execution
    dynamic typed -- no need to declare variables types
    exetensive liberies -- numpy,pandas,Djanjo
    versitile -- used in AI,ML,Automation,data science....

## print()

    print is a built in function that displayes text in on the screen.

## input()
    
    pauses the program and wait for user input.
    returns the input as a str by defult.

## common types

    int,float,boolean,str

## comment line

    single line comment --  # code
    multi line comment --  """   code...  """

## variables

    a variable ia name ref  that pointa to a value stored in memory

30/10/2025

## data types

    text type -- str
    numeric type -- int,float,complex
    sequence type  -- list,tuple,range
    mapping type -- dictonary
    set type -- set,frozenset
    boolean type -- bool
    binary type -- bytes,bytearray, memoryview
    none type -- none

## operators type

    Arithmetic , assignement,comparsion or relational,
    logical,membership,bitwise operators

    Arithmetic 

        +,-,*,/,%,**,//
    
    assignement
        =
        +=,-=,*=,/=,%=,**=,//=,

    Comparison operatior

        ==,!=,>=,<=,<,>
    
    logical operatior
     
        &&,||,!

        && and
            t f=f
            f t=f
            f f=f
            t t=t
        
        || or

            t f=t
            f t=t
            f f=f
            t t=t
        ! not

            t=f
            f=t

31/10/2025

membership operatior

    in   , not in   

bitwise operators

    &-and ,|-or ,^ - xor, ~ - not

    8 = 1000
    3 = 0011
        1011=2

list
  
    List is a sequence of collection data

    we change the data


03/11/2025

##conditional statements

    if,else,elif
    nested

    syntex:

    if condition:
        //code
    elif condition:
        //code
    else:
        //code

4/11/2025

## loop and nested loop 

    a loops allowes you to execute a block of code repeatedly as log as a conditonal is true.

    for loop -- used to iterate over a sequence (list,string,rande)
        syntex

            for variableName in sequence:
                //code

    while loop -- runs until acondition become false.

        syntex

            while condition:
                //code

## break,continue statement

    break-- used to exit the loop immediatly ,even if the condition is till true.

    continue-- used to skip the rest of code in the current loop interation 
    and conndition with the next one.

## else block with loop

    The else part executes only if the loop is not broken  using break.

5/11/2025

## data structure

    list ,tuple, dictonary,set

    list-mutable,[]

    tuple-immutable,()
        data cannot be modify.
        faster than list
        used for fixed data.

    set- mutable ,{}
        does not allow the duplicate values.

    Dectionary- key-immuatable,value-muatable,{}
        a dictionary stores data in a key-vaues paris.
        {key:value}

    nested dictionary

6/11/2025

## function and scope 

    function--
        a function is a block of reuasble code that can 
        perform a specific task.

    syntax: 

        def function_name(parameters)
            // code
        
        function_name()

    function with parameter and argument
    function with return value
    tyes of functional arguments
        positional arg
        keyword arg
        default arg
        variable length arg
            *arg-- multiple positional arg
            **kwargs--multiple keyword arg
9/11/2025
    scope of variable
        local scope
        global scope
        modify global variable inside a function
    nested function

10/11/2025

String and string functions in python

    # string slicing
    # step slicing
    # string concatenation
    # srting function
    # string Comparison
    # checking char in str

11/11/2025

## Exceptional handling

    when something unexpected happens in your code 
    (like dividing by 0,opening a missing fileor wrong input),pyhton throwns an error..

    Exceptional handleing lets you handle those errors gracefully -- without crasing the program.


 syntexx:

        try:
            //code
        except ErrorType:
            // code
        else:
            //code
        finally:
            //code

12/11/2025

## oops

 object -oriented -programmming is a way of writing program where you model
 real-world entites..

 # attributes(data)== variables that describes it...
 # Method (behaviour) == function that defines its actions..

    real-- world        python

        car      --       class
        specificcar[bmw]--object
        car color,model-- attributes
        start(),stop()--  Method(def)

# class and object
    a class is a bluePrint for creating object
    An object is an instance of that class

# constructor  _init_()
    the constructor is a special  method in python that autommatically runs 
    when you create an object.

    def _init_(self,parameter1,parameter2...): 

# class variable and instance variable

    instance variable== belongs to each object
    class variable == shared by all object

# types  of method in class

     instance Methods-- works on object
     class method-- works on class  data
     static Method -- general utility (no access to class or instances)

# Encaptulation (Data protection)

     Encaptulation means hiding data and controlling access 
     usinng private variables.

# Inheritance (reusing code)

    Inheritance allows a class (child) to reuse the 
    properties and Methods of another class (parent).

# Polymorphism

Polymorphism means same function name but 
different behaviour on the object.





  